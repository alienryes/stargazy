# Installed into the display's own virtualenv (see setup.sh), which is created
# with --system-site-packages so pillow/numpy/requests come from apt rather than
# being rebuilt here.
pillow
numpy
requests

# Only needed when weather.source = "direct". pyastroweatherio's published
# package declares NO dependencies of its own - its setup.py reads
# requirements.txt at build time and that file is not in the sdist - so every
# one of its imports has to be listed here by hand or the import fails at
# runtime on aiofiles. Roughly 195MB installed, most of it pandas.
pyastroweatherio
aiofiles
aiohttp
pyephem
pytz
typeguard
openmeteo-requests
pandas
tabulate
