# Installed into the display's own virtualenv (see setup.sh), which is created
# with --system-site-packages so pillow/numpy/requests come from apt rather than
# being rebuilt here.
pillow
numpy
requests

# Needed by BOTH weather sources on this build: the targets page computes alt-az
# from right ascension and declination, and the meteor page needs the Sun's
# solar longitude. It happens to arrive with pyastroweatherio as well, but a
# build configured for Home Assistant installs none of that, so it is listed
# here in its own right rather than relied on as somebody else's dependency.
ephem

# Only needed when weather.source = "direct". pyastroweatherio's published
# package declares NO dependencies of its own - its setup.py reads
# requirements.txt at build time and that file is not in the sdist - so every
# one of its imports has to be listed here by hand or the import fails at
# runtime on aiofiles. Roughly 195MB installed, most of it pandas.
pyastroweatherio
aiofiles
aiohttp
pyephem
pytz
typeguard
openmeteo-requests
pandas
tabulate
